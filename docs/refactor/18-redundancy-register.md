# Registro de redundancias canónicas

| ID | Ubicación | Tipo | Semántica e impacto | Riesgo | Decisión | Evidencia / prueba |
| --- | --- | --- | --- | --- | --- | --- |
| RED-001 | `recibos.estado/intentos/ultimo_error` frente a `recibos_pendientes` | datos/estado | Dos máquinas describían el mismo trabajo técnico. | Estados contradictorios y doble conteo. | Eliminar del documento; la outbox conserva estado, intentos y error. | V1 + `ReciboStorageService`; schema validate. |
| RED-002 | `cargos.updated_at` | datos | Default de creación sin ruta que actualizara el valor. | Timestamp engañoso. | Eliminar. | Búsqueda de consumidores vacía; Flyway/Hibernate verdes. |
| RED-003 | `PagoResponse` usado en listado | payload/consulta | Cada fila cargaba aplicaciones, saldos de cargos y crédito aunque la pantalla usa cuatro campos. | N+1 y payload variable. | Sustituir por `PagoResumenResponse`. | `PagosPagina` usa id/fecha/monto/estado; compile/build verdes. |
| RED-004 | `RefreshOnNavigation` | frontend/proceso | Recargaba toda la SPA en cada ruta, invalidando cache y repitiendo bootstrap. | Requests y estado duplicados. | Eliminar; React Router y React Query son suficientes. | Frontend build/test. |
| RED-005 | estado remoto de pagos/egresos/cargos/métodos en `useState` | frontend | Copiaba respuestas remotas fuera de React Query. | Cache doble y recargas manuales. | React Query como autoridad; estado local sólo para formulario/filtro. | `queryKeys.ts`, páginas canónicas. |
| RED-006 | links de navegación sin ruta | frontend | Entradas a débitos, pagos heredados, planillas y liquidaciones eliminadas. | Navegación a pantalla inexistente. | Eliminar; no restaurar pantallas. | Comparación `navigation.ts` vs `routes.ts`. |
| RED-007 | `IBonificacionServicio`, `IRolServicio`, `IUsuarioServicio`, `IInscripcionServicio` | código | Interfaces locales sin segunda implementación ni frontera. | Ceremonia y archivos extra. | Eliminar. | Búsqueda de consumidores; compile verde. |
| RED-008 | `INotificacionService` | código | Interfaz vacía sin implementación/consumidor. | Código muerto. | Eliminar. | `rg`; compile verde. |
| RED-009 | `EmailControlador`, `MatriculaScheduler` | código/endpoint | Clases vacías; el scheduler real está en `ScheduledTasks`. | Endpoint/abstracción falsa. | Eliminar. | Búsqueda de consumidores; compile verde. |
| RED-010 | `@faker-js/faker`, `lodash`, `@types/lodash`, `react-paginate` directos | dependencia | Ningún import en `frontend/src`. | Installs y superficie innecesarios. | Eliminar dependencias directas. | `rg`; `npm ci`, lint/test/build. |
| RED-011 | `AplicacionPagoRepositorio.flush()` explícito | escritura | La consulta agregada posterior ya fuerza AUTO flush. | Operación explícita redundante. | Eliminar. | Pago PostgreSQL y concurrencia verdes. |
| RED-012 | `DetallePago`, clones y campos `es_clon` | datos/código/proceso | Duplicaban obligación, aplicación y pagos parciales. | Historia contradictoria. | Ya eliminados; impedir regresión. | Catálogo PostgreSQL y búsqueda estructural. |
| RED-013 | saldo de crédito en alumno | datos/estado | Saldo mutable duplicaría el ledger. | Divergencia financiera. | Derivar de `movimientos_credito`. | `saldoByAlumnoId`, V1 sin columna. |
| RED-014 | saldo de cargo | datos/estado | Total persistido duplicaría aplicaciones/consumos. | Sobrepago o estado incoherente. | Derivar; sólo `estado` queda como proyección. | SQL `04/05`, `CargoServicio.saldo`. |
| RED-015 | `Cargo.estado` | estado | Facilita listados pero deriva del saldo y anulación. | Divergencia si aparece otra ruta. | Proyección justificada: una ruta transaccional y reconciliación PostgreSQL. | `CargoServicio.actualizarEstado`, tests de pago concurrente, auditoría. |
| RED-016 | `stocks.cantidad_actual` y ledger | datos/estado | La cantidad es lectura operativa; los movimientos explican historia. | Dos autoridades si otra clase escribe cantidad. | Proyección justificada; sólo `StockServicio` actualiza ambos bajo lock. | búsqueda de setters, `FIN-STOCK-PROYECCION`. |
| RED-017 | totales diarios/mensuales/por método de caja | datos | Son agregaciones del ledger. | Tablas de resumen divergentes. | No persistir; calcular en `CajaServicio`. | V1 no contiene tabla de totales. |
| RED-018 | `Mensualidad.descripcion`, `Cargo.descripcion` | datos | Parecen duplicadas, pero capturan conceptos en momentos distintos. | Cambio retroactivo si se sincronizan. | Snapshot válido e inmutable; no FK. | Modelo y recibo usan descripción de cargo sólo para mostrar. |
| RED-019 | `Disciplina.valor_cuota` / `Cargo.importe_original` | datos | Una es regla vigente; otra, importe emitido. | Recalcular historia con precio actual. | Mantener ambas con semántica documentada. | `MensualidadServicio` emite cargo una vez por unique. |
| RED-020 | `Stock.precio` / `VentaStock.precio_unitario` | datos | Precio vigente frente a precio de la venta. | Destruir historia al editar catálogo. | Snapshot válido. | `StockServicio.vender`. |
| RED-021 | `Pago.monto_recibido`, suma aplicada, crédito | datos | Son conceptos relacionados, no equivalentes. | Dinero sin destino. | Persistir sólo monto; derivar aplicaciones/crédito y auditar ecuación. | `PagoCanonicoPostgreSqlTest`, SQL financiera. |
| RED-022 | estado/fecha de reversión de aplicación | estado | Estado representa vigencia; fecha/motivo preservan hecho histórico. | Borrar historia si se reemplaza por delete. | Mantener; no son duplicados semánticos. | constraint `ck_aplicaciones_reversion`. |
| RED-023 | `activo`, `estado`, `fecha_baja` | estado | Potencial triple representación en entidades históricas. | Combinaciones imposibles. | Alumno usa activo+fecha; inscripción usa estado+fecha; no entidad conserva los tres equivalentes. | checks V1. |
| RED-024 | request hash e idempotency key | datos | Key identifica operación; hash identifica su contenido. | Reutilización silenciosa con payload distinto. | Mantener ambos en pago, egreso, venta y crédito, incluidas reversiones. | `RequestHash`, constraints y reintentos validados. |
| RED-025 | listados con “infinite scroll” | frontend/consulta | Salones y reportes anexaban páginas; otros listados ocultaban elementos por observer. | Reconstrucción total y requests invisibles. | Salones reemplaza página; reporte usa contrato canónico; carga local sólo por botón `Mostrar más`. | Sin `IntersectionObserver`/`InfiniteScroll`; frontend gates. |
| RED-030 | Chart.js, react-chartjs-2, file-saver y tipos | dependencia | Sólo sostenían un reporte contra endpoints retirados. | Dependencias y bundle sin consumidor válido. | Eliminar y usar descarga Blob nativa. | `npm ci`, lint, tests y build. |
| RED-031 | mapper/request de matrícula y DTOs de reporte heredados | código/DTO | No tenían controlador, servicio ni pantalla consumidora. | Contratos falsos. | Eliminar; mantener sólo DTOs canónicos. | referencias, compile y build. |
| RED-027 | endpoints de inscripción y alumno sin consumidor | API/proceso | `bulk`, estadísticas, filtro por disciplina, inscripción singular activa, alumno “datos” y listado simplificado duplicaban rutas o devolvían grafos. | Más contratos, consultas completas y orquestación duplicada. | Eliminar; conservar CRUD, lista paginada y activas por alumno. | Búsqueda de consumidores, compile frontend/backend. |
| RED-028 | suma de caja en memoria | consulta/proceso | Cargaba todos los movimientos del período para sumar y responder. | Memoria, payload y tiempo proporcionales al historial. | Agregar totales en SQL y paginar movimientos; ledger sigue siendo autoridad. | `MovimientoCajaRepositorio.totales`, prueba PostgreSQL de pago/reversión. |
| RED-029 | `alumnos.cuit` | datos/payload | Columna y propiedad sin request, response ni consumidor. | Información inaccesible que podía divergir de documento. | Eliminar de entidad y V1. | Búsqueda de consumidores vacía; Hibernate validate. |
| RED-026 | mapper warnings | código/mapeo | Varios MapStruct ignoraban campos implícitamente y la asistencia diaria mapeaba una inscripción como alumno. | Contratos incompletos y payload incorrecto. | Resolver mapper por mapper: referencias gestionadas se ignoran explícitamente, defaults se expresan y asistencia usa `inscripcion.alumno`; sin política global de silencio. | Compilación Maven sin warnings MapStruct. |

## Resultado

La palabra “redundante” se reserva para semántica equivalente. Las copias que
preservan historia (`Cargo.importe_original`, descripciones y precio unitario)
se mantienen; las proyecciones se aceptan sólo con ruta única y reconciliación.
